import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
// import Coffee from "./coffee";

ReactDOM.createRoot(document.getElementById("root")).render(<App />);

// main.jsx:

// The main.jsx file, or sometimes named index.jsx, is often used as the entry point for your React application.
// It might contain the initialization code, where the root component is rendered into the DOM using ReactDOM.
